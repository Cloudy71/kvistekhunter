using UnityEngine;

public class TaskPlaceholder : MonoBehaviour {
    public string                        Group; // If the task should stay in some group area.
    public GameLocalTask.LocalTaskType[] Types; // Specify task types
}